name: Flutter CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Flutter
        uses: subosito/flutter-action@v5
        with:
          flutter-version: '3.13.0' # Replace with your desired Flutter version
          channel: 'stable'

      - name: Get Dependencies
        run: flutter pub get

      - name: Run Unit Tests
        run: flutter test

      - name: Build APK
        run: flutter build apk

      # Add more steps as needed, such as code signing or publishing artifacts.

      # # Example of publishing APK as an artifact:
      # - name: Publish APK Artifact
      #   uses: actions/upload-artifact@v2
      #   with:
      #     name: app-release
      #     path: build/app/outputs/flutter-apk/app-release.apk

